apply plugin: "war"

bootWar {
    mainClass = "io.citizenjournalist.as.AsCmsTranslationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
